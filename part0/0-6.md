sequenceDiagram
    participant user
    participant browser
    participant server

    Note left of user: The user types a note and clicks "Save"

    Note right of browser: The JS intercepts the form submission using e.preventDefault()
    Note right of browser: The JS creates the new note object, adds it to the local array, and re-renders the UI immediately

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note right of browser: The request payload contains the new note in JSON format: {content: "16-06-2026 11:10 GMT +1", date: "2026-06-16T10:10:04.575Z"}
    server-->>browser: 201 Created - message: "note created"
    deactivate server

    Note right of browser: The browser stays on the same page without reloading. No further GET requests are needed.